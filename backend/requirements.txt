# ---------------------------------------------------------------------------
# Braintuitive - backend dependencies
# Python 3.11+ (developed against 3.13)
# ---------------------------------------------------------------------------

# --- Web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # multipart/form-data (PDF uploads)

# --- Configuration & validation -------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Relational database ---------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0                   # migrations, once the schema starts moving

# --- Document database -----------------------------------------------------
motor==3.6.0                      # async MongoDB driver
pymongo==4.9.2

# --- Authentication --------------------------------------------------------
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-jose[cryptography]==3.3.0  # JWT encode/decode
email-validator==2.2.0

# --- LangChain / LLM (Nebius is OpenAI-wire-compatible) --------------------
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-openai==0.2.14
openai==1.59.6

# --- RAG: vectors, embeddings, PDF ----------------------------------------
pinecone-client==5.0.1
tiktoken==0.8.0                   # token-accurate chunking
pypdf==5.1.0
pdfplumber==0.11.4                # better table/layout extraction than pypdf

# --- Scheduling (no Redis / Celery by design) ------------------------------
APScheduler==3.11.0

# --- HTTP (You.com search API, Nebius REST fallbacks) ----------------------
httpx==0.28.1
tenacity==9.0.0                   # retry/backoff around flaky provider calls

# --- Charts (no external AI image generation) ------------------------------
matplotlib==3.10.0
plotly==5.24.1

# --- Dev / test ------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.2
black==24.10.0
ruff==0.8.6
